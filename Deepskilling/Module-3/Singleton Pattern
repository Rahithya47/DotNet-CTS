class StudentDatabase:

    _instance = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance

db1 = StudentDatabase()
db2 = StudentDatabase()

print("Only one instance exists:", db1 is db2)
